# حالة البناء والأصول

## فحوص المصدر المكتملة
- `package.json` يستخدم أرقام إصدارات دقيقة فقط.
- `packageManager` مثبت على `pnpm@11.25.0`.
- Node مثبت على `24.20.0` في `engines` و`.node-version`.
- لا يوجد `pnpm-workspace.yaml`.
- تكامل sitemap مشروط بوجود Astro `site` الحقيقي، ولا توجد قائمة URL مكتوبة يدوياً.
- canonical وOpen Graph وJSON-LD URLs مشتقة من `Astro.site` فقط، وتُحذف عند عدم وجود النطاق.
- لا توجد صور hotlink في صفحات الموقع؛ كل عناصر `<img>` تشير إلى `/images/...` محلياً.
- GA4 لا يبدأ قبل موافقة المستخدم على التحليلات.
- صفحات الخصوصية وشروط الخدمة وإعدادات ملفات تعريف الارتباط صفحات مستقلة وليست نوافذ منبثقة.
- واجهة المستخدم كلها عربية و`lang="ar-SA"` و`dir="rtl"`.
- الخريطة مضبوطة على العربية والسعودية.

## فحص التثبيت النظيف
تمت محاولة الأمر المطلوب بعد حذف `node_modules` و`dist`:

`CI=1 corepack pnpm install --frozen-lockfile`

بيئة التنفيذ الحالية تمنع اتصال الحاوية بالإنترنت، لذلك يتوقف Corepack / مدير الحزم قبل تنزيل pnpm أو الحزم. هذا قيد في شبكة بيئة التنفيذ وليس خطأ تبعيات تم اكتشافه داخل المشروع. انظر `VALIDATION-INSTALL.log`.

بسبب هذا القيد لا يمكن الادعاء بأن `pnpm check` و`pnpm build` نُفذا بنجاح في هذه الحاوية. تم الاحتفاظ بـ `pnpm-lock.yaml` من مجموعة إصدارات متطابقة سابقة، وتُجرى فحوص مصدر إضافية محلياً داخل الحاوية.

## حالة الصور الحقيقية
تم التحقق من صورة حقيقية للمكان على Wikimedia Commons بترخيص CC BY-SA 4.0، إضافة إلى مصادر فوتوغرافية أخرى مرتبطة بالمكان. لكن الحاوية نفسها تمنع تنزيل الملفات الثنائية من الويب، ولذلك لا توجد حالياً بايتات JPG حقيقية داخل هذا التسليم.

لم يتم استبدال الصور بصور مولدة بالذكاء الاصطناعي ولم يتم استخدام hotlink خفي. يعرض الموقع طبقة احتياطية محلية إذا كانت ملفات JPG غير موجودة.
